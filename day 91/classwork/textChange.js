export default function toUppercase(text) {
    return text.toUpperCase();
}

export function reversed(text) {
    return text.split('').reverse().join('');
}
export function addExclamation(text) {
    return text + '!';
}